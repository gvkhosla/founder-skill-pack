{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "generated/**"]
    },
    "check": {
      "dependsOn": ["^check"],
      "outputs": []
    },
    "lint": {
      "dependsOn": ["^lint"],
      "outputs": []
    },
    "eval": {
      "dependsOn": ["^eval"],
      "outputs": [".evals/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
