# Founder Skills CLI (future workspace app)

The current published CLI still lives at the repo root.

This folder is the future workspace location for the multi-package Founder Skills OS CLI, including:
- install flows
- host generation
- state operations
- sequence operations
- doctor / validation commands
